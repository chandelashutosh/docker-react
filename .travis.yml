sudo: required
services:
  - docker

before_install:
  - docker build -t chandelashutosh30/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true chandelashutosh30/docker-react npm run test -- --coverage

language: generic
  